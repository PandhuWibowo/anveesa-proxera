# Frontend environment variables
# Copy to .env and fill in values

# Backend API base URL (leave empty to use Vite dev proxy)
VITE_API_URL=

# WebSocket URL (leave empty to auto-detect from location.host)
VITE_WS_URL=
